// Package repository contains interfaces for data access operations.
package repository

import (
	"{{.ModuleName}}/internal/dto/request"
	"{{.ModuleName}}/internal/entity"
)

// {{.SampleAPIName}}Repository defines the interface for {{.SampleAPINameLower}} data operations
type {{.SampleAPIName}}Repository interface {
	// Create operations
	Create(item *entity.{{.SampleAPIName}}) error

	// Read operations
	GetByID(id int64) (*entity.{{.SampleAPIName}}, error)
	List(req request.List{{.SampleAPIName}}sRequest) ([]entity.{{.SampleAPIName}}, int64, error)
	Count() (int64, error)
	CountByStatus(status entity.{{.SampleAPIName}}Status) (int64, error)

	// Update operations
	Update(item *entity.{{.SampleAPIName}}) error
	UpdateStatus(id int64, status entity.{{.SampleAPIName}}Status) error
	BulkUpdateStatus(ids []int64, status entity.{{.SampleAPIName}}Status) (int, []int64, error)

	// Delete operations
	Delete(id int64) error

	// Utility operations
	ExistsByID(id int64) (bool, error)
}

// DatabaseRepository defines the interface for database health operations
type DatabaseRepository interface {
	Ping() error
	Close() error
}
