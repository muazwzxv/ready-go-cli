-- name: Get{{.SampleAPIName}}ByID :one
SELECT * FROM {{.SampleTableName}} WHERE id = ?;

-- name: List{{.SampleAPIName}}s :many
SELECT * FROM {{.SampleTableName}}
ORDER BY created_at DESC
LIMIT ? OFFSET ?;

-- name: Create{{.SampleAPIName}} :execresult
INSERT INTO {{.SampleTableName}} (name, description, status, created_at, updated_at)
VALUES (?, ?, ?, NOW(), NOW());

-- name: Update{{.SampleAPIName}} :exec
UPDATE {{.SampleTableName}}
SET name = ?, description = ?, status = ?, updated_at = NOW()
WHERE id = ?;

-- name: Delete{{.SampleAPIName}} :exec
DELETE FROM {{.SampleTableName}} WHERE id = ?;

-- name: Count{{.SampleAPIName}}s :one
SELECT COUNT(*) FROM {{.SampleTableName}};

-- name: Get{{.SampleAPIName}}sByStatus :many
SELECT * FROM {{.SampleTableName}}
WHERE status = ?
ORDER BY created_at DESC;
