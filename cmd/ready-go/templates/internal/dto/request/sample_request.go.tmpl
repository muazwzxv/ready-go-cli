// Package request contains structures for API request payloads.
package request

// Create{{.SampleAPIName}}Request represents the request payload for creating a new {{.SampleAPINameLower}}
type Create{{.SampleAPIName}}Request struct {
	Name        string `json:"name" validate:"required,min=1,max=255"`
	Description string `json:"description" validate:"max=1000"`
	Status      string `json:"status" validate:"omitempty,oneof=active inactive archived"`
}

// Update{{.SampleAPIName}}Request represents the request payload for updating an existing {{.SampleAPINameLower}}
type Update{{.SampleAPIName}}Request struct {
	Name        *string `json:"name,omitempty" validate:"omitempty,min=1,max=255"`
	Description *string `json:"description,omitempty" validate:"omitempty,max=1000"`
	Status      *string `json:"status,omitempty" validate:"omitempty,oneof=active inactive archived"`
}

// Bulk{{.SampleAPIName}}Request represents the request payload for bulk operations
type Bulk{{.SampleAPIName}}Request struct {
	IDs    []int64 `json:"ids" validate:"required,min=1,max=100"`
	Status string  `json:"status" validate:"required,oneof=active inactive archived"`
}
