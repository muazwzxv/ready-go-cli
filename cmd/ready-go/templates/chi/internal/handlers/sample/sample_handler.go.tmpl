package {{.SampleAPINameLower}}

import (
	"database/sql"
	"net/http"
	"strconv"

	"github.com/go-chi/chi/v5"
	"github.com/redis/go-redis/v9"
	"{{.ModuleName}}/internal/handlers/util"
	"{{.ModuleName}}/internal/models"
)

type GetByIDHandler struct {
	DB      *sql.DB
	Queries *models.Queries
	Redis   *redis.Client
}

func (h *GetByIDHandler) Handle(w http.ResponseWriter, r *http.Request) {
	// Read URI param
	idStr := chi.URLParam(r, "id")
	id, err := strconv.Atoi(idStr)
	if err != nil {
		util.HandleError(w, util.BuildErrorWithCode(
			http.StatusBadRequest,
			"Invalid ID format",
			"INVALID_ID_FORMAT",
		))
		return
	}

	// TODO: Implement your logic here
	// Example: result, err := h.Queries.Get{{.SampleAPIName}}(r.Context(), h.DB, int32(id))

	util.WriteJSON(w, http.StatusOK, util.SuccessResponse{Data: id})
}
